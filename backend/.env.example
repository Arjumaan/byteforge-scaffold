# SECURITY CONFIGURATION
# Generate a strong key using: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your-super-secret-master-key-here

# JWT Configuration
JWT_SECRET=your-jwt-signing-secret-here

# Database
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/byteforge

# Infrastructure
REDIS_URL=redis://localhost:6379/0

# Allowed Hosts (Security Middleware)
ALLOWED_HOSTS=localhost,127.0.0.1
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
